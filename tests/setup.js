 import '@testing-library/jest-dom/vitest';

// Пример глобальных моков, если понадобятся:
// vi.stubGlobal('IntersectionObserver', class { observe(){} disconnect(){} });
// vi.stubGlobal('ResizeObserver', class { observe(){} disconnect(){} });
